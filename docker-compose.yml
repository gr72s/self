services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "58000:8000"
    volumes:
      - ./backend:/app
      - ${SELF_HOME}:/root/.self:rw
    environment:
      - PYTHONUNBUFFERED=1
      - SELF_ENV=production
    restart: unless-stopped
