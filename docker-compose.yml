services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ~/.self:~/.self
    environment:
      - PYTHONUNBUFFERED=1
      - SELF_ENV=production
    restart: unless-stopped
