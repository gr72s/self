<view class="page-container">
  <view class="container">
    <!-- Header -->
    <view class="header">
      <text class="greeting">你好{{user && user.username ? ', ' + user.username : ''}}!</text>
      <view class="quick-action" bindtap="startNewWorkout">
        <text class="action-text">开始训练</text>
      </view>
    </view>

    <!-- Category Cards -->
    <view class="category-grid">
      <view class="category-card card-orange" bindtap="navigateToWorkouts">
        <text class="card-icon">🏋️</text>
        <text class="card-title">训练管理</text>
        <text class="card-stat">{{workouts.length}} 次</text>
      </view>

      <view class="category-card card-pink" bindtap="navigateToRoutines">
        <text class="card-icon">📋</text>
        <text class="card-title">训练计划</text>
        <text class="card-stat">{{uniqueRoutines}} 个</text>
      </view>

      <view class="category-card card-purple" bindtap="navigateToExercises">
        <text class="card-icon">💪</text>
        <text class="card-title">动作库</text>
        <text class="card-stat">{{totalExercises}} 个</text>
      </view>

      <view class="category-card card-blue" bindtap="navigateToGyms">
        <text class="card-icon">🏢</text>
        <text class="card-title">健身房</text>
        <text class="card-stat">管理</text>
      </view>

      <view class="category-card card-green" bindtap="navigateToMuscles">
        <text class="card-icon">🎯</text>
        <text class="card-title">肌肉群</text>
        <text class="card-stat">查看</text>
      </view>

      <view class="category-card card-yellow" bindtap="navigateToProfile">
        <text class="card-icon">⭐</text>
        <text class="card-title">目标设定</text>
        <text class="card-stat">设置</text>
      </view>
    </view>
  </view>
</view>